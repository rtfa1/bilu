# Phase 04 — Search UI

## Goal

Provide `/` search with quick navigation between matches.

## Checklist

- [ ] `/` opens a prompt line and captures a query.
- [ ] Filter the visible task list by query (title + description).
- [ ] `n` next match, `p` previous match.
- [ ] Display search state in status bar.

## Acceptance

- Search feels immediate and keeps selection consistent where possible.

